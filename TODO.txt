BUG:
    STDIN in PKr-Cli use ScanLine/ReadLine instead of Scan
    Cuz if Workspace Name has space in it then it won't be able to read whole Name
    "Just TimePass" => "Just"

---------------------------------------------------------------

Future Updates:
1. Also add a list of "Avoid files" like ".env" or any other secret files

2. Listeners can fetch workspace from other listeners

---------------------------------------------------------------
